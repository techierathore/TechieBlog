# Story 7.7: Add Newsletter Management in Admin

## Status
Draft

## Story
**As an** administrator,
**I want** to manage newsletter subscribers in the admin panel,
**so that** I can view, export, and manage my subscriber list.

## Acceptance Criteria

1. **AC1:** Admin menu includes "Subscribers" link
2. **AC2:** Subscribers list page displays all subscribers with email, date, status
3. **AC3:** Can filter subscribers by active/inactive status
4. **AC4:** Can search subscribers by email
5. **AC5:** Can toggle subscriber active status
6. **AC6:** Can export subscriber list to CSV
7. **AC7:** Displays total subscriber count

## Tasks / Subtasks

- [ ] Task 1: Add Subscribers menu item (AC: 1)
  - [ ] Update AdminLayout.razor to include Subscribers link
  - [ ] Use appropriate icon (e.g., Mail or People)

- [ ] Task 2: Create SubscribersList.razor page (AC: 2, 7)
  - [ ] Create admin page at /admin/subscribers
  - [ ] Display data grid with Email, SubscribedOn, IsActive columns
  - [ ] Show total count in page header

- [ ] Task 3: Add filtering functionality (AC: 3, 4)
  - [ ] Add status filter tabs (All, Active, Inactive)
  - [ ] Add search box for email search
  - [ ] Implement ApplyFilter method

- [ ] Task 4: Add status toggle (AC: 5)
  - [ ] Add toggle/switch in each row
  - [ ] Call SubscriberSvc.UpdateStatus method
  - [ ] Show success/error feedback

- [ ] Task 5: Implement CSV export (AC: 6)
  - [ ] Add Export button to page header
  - [ ] Generate CSV with Email, SubscribedOn columns
  - [ ] Trigger file download

- [ ] Task 6: Extend SubscriberSvc (AC: 3, 4, 5, 6)
  - [ ] Add GetAllSubscribers method
  - [ ] Add SearchSubscribers method
  - [ ] Add UpdateSubscriberStatus method
  - [ ] Add GetSubscribersForExport method

## Dev Notes

### Relevant Files
- `source/BlogUI/Layouts/AdminLayout.razor` - Admin menu
- `source/BlogUI/Pages/AdminPages/` - Admin pages
- `source/BlogEngine/Services/SubscriberSvc.cs` - Subscriber service (from 7.6)

### Page Structure (Similar to SeriesList.razor)
```razor
@page "/admin/subscribers"
@attribute [Authorize(Policy = "AdminOnly")]
@layout BlogUI.Layouts.AdminLayout

<div class="subscribers-page">
    <div class="page-header">
        <h1>Subscribers</h1>
        <FluentButton OnClick="ExportToCsv">Export CSV</FluentButton>
    </div>
    
    <div class="filters-row">
        <div class="status-tabs">...</div>
        <div class="search-box">...</div>
    </div>
    
    <table class="data-grid">...</table>
</div>
```

### CSV Export Implementation
```csharp
private async Task ExportToCsv()
{
    var subscribers = SubscriberService.GetSubscribersForExport();
    var csv = "Email,SubscribedOn,Status\n";
    foreach (var s in subscribers)
    {
        csv += $"{s.Email},{s.SubscribedOn:yyyy-MM-dd},{(s.IsActive ? "Active" : "Inactive")}\n";
    }
    // Use JS interop to download file
}
```

### Dependencies
- Story 7.6 must be completed first (creates Subscriber model and base service)

## Testing

### Test Standards
- Login as admin
- Navigate to /admin/subscribers
- Verify subscriber list displays
- Test status filter tabs
- Test email search
- Toggle a subscriber's status
- Export to CSV and verify file contents

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2024-12-25 | 1.0 | Initial story creation | Sarah (PO) |
