# Story 1.5: CSS Theme Infrastructure Setup

## Status

Done

## Story

**As a** developer,
**I want** CSS variable-based theming infrastructure established,
**so that** themes can be customized without code changes.

## Acceptance Criteria

1. CSS custom properties file created based on `mockups/styles.css` variables (colors, fonts, spacing)
2. "Fluent Modern" theme (default) derived from `mockups/styles.css` with light/dark variants
3. "Developer Dark" theme derived from `mockups/theme-developer.css`
4. "Minimal Clean" theme derived from `mockups/theme-minimal.css`
5. Theme variables applied to base layout and components
6. Theme switching mechanism implemented via configuration
7. Documentation comments explain each variable's purpose

## Tasks / Subtasks

- [x] Task 1: Create base CSS custom properties file (AC: 1, 7)
  - [x] Create `source/BlogUI/Themes/_variables.css` with all CSS custom properties
  - [x] Extract color palette variables from `mockups/styles.css` (:root section)
  - [x] Extract typography variables (font-family, font-size-*, font-weight-*, line-height-*)
  - [x] Extract spacing scale variables (spacing-xs through spacing-3xl)
  - [x] Extract layout variables (max-content-width, sidebar-width, header-height, border-radius-*)
  - [x] Extract shadow variables (shadow-sm, shadow-md, shadow-lg)
  - [x] Extract transition variables (transition-fast, transition-normal)
  - [x] Add documentation comments explaining each variable category and purpose

- [x] Task 2: Create Fluent Modern theme with light/dark variants (AC: 2)
  - [x] Create `source/BlogUI/Themes/fluent-modern.css`
  - [x] Define light theme colors as default (matching `mockups/styles.css` :root)
  - [x] Define dark theme colors using `[data-theme="dark"]` selector (matching mockups/styles.css dark section)
  - [x] Include system preference detection using `@media (prefers-color-scheme: dark)`
  - [x] Add theme-toggle button styles for light/dark switching
  - [x] Add documentation comments for theme characteristics

- [x] Task 3: Create Developer Dark theme (AC: 3)
  - [x] Create `source/BlogUI/Themes/developer-dark.css`
  - [x] Copy `[data-site-theme="developer"]` variables from `mockups/theme-developer.css`
  - [x] Include syntax highlighting color variables (keyword, string, comment, function, variable, number)
  - [x] Include both light and dark mode variants using combined selectors
  - [x] Override typography to use monospace font families (JetBrains Mono, Fira Code)
  - [x] Add documentation comments describing IDE/code editor inspiration

- [x] Task 4: Create Minimal Clean theme (AC: 4)
  - [x] Create `source/BlogUI/Themes/minimal-clean.css`
  - [x] Copy `[data-site-theme="minimal"]` variables from `mockups/theme-minimal.css`
  - [x] Include typography-focused settings (Charter serif, Playfair Display headings)
  - [x] Include both light and dark mode variants
  - [x] Include article-max-width variable for narrower content
  - [x] Add documentation comments describing Medium/Substack inspiration

- [x] Task 5: Create ThemeService for theme management (AC: 6)
  - [x] Create `source/BlogUI/Common/ThemeService.cs`
  - [x] Implement `GetCurrentTheme(): string` to retrieve from LocalStorage
  - [x] Implement `SetTheme(themeName: string): void` to persist to LocalStorage
  - [x] Implement `ToggleDarkMode(): void` to switch light/dark
  - [x] Implement `GetAvailableThemes(): List<ThemeInfo>` returning fluent-modern, developer-dark, minimal-clean
  - [x] Inject `Blazored.LocalStorage` for persistence
  - [x] Add XML documentation comments for all public methods
  - [x] Register service in `source/TechieBlog/Program.cs`

- [x] Task 6: Create ThemeProvider component (AC: 5, 6)
  - [x] Create `source/BlogUI/Components/ThemeProvider.razor`
  - [x] Inject ThemeService to manage state
  - [x] Apply `data-theme` attribute to document root for light/dark mode
  - [x] Apply `data-site-theme` attribute for theme selection (fluent-modern, developer, minimal)
  - [x] Load CSS theme files dynamically based on selected theme
  - [x] Add comprehensive Razor comment documentation

- [x] Task 7: Integrate themes into layouts (AC: 5)
  - [x] Update `source/BlogUI/App.razor` to wrap content in ThemeProvider
  - [x] Update `source/BlogUI/Layouts/MainLayout.razor` to use CSS variables
  - [x] Update `source/BlogUI/Layouts/AdminLayout.razor` to use CSS variables
  - [x] Update `source/BlogUI/Layouts/AuthLayout.razor` to use CSS variables
  - [x] Ensure layouts reference theme CSS files

- [x] Task 8: Update _Host.cshtml for theme CSS loading (AC: 5)
  - [x] Add link references for theme CSS files in `source/TechieBlog/Pages/_Host.cshtml`
  - [x] Ensure CSS files load in correct order (_variables.css first, then theme files)
  - [x] Add inline script for flash-of-unstyled-content prevention (read theme from localStorage on load)

- [x] Task 9: Build and verify theme infrastructure (AC: 1, 2, 3, 4, 5, 6)
  - [x] Run `dotnet build` - verify 0 errors, 0 warnings
  - [x] Verify themes load correctly on application start
  - [x] Verify light/dark mode toggle works
  - [x] Verify theme switching between all three themes works
  - [x] Verify CSS variables apply to all layout components

## Dependencies

- **Story 1.4** (Fluent UI Blazor Integration) - Done

## Dev Notes

### Previous Story Insights
From Story 1.4 implementation:
- Build succeeds with 0 errors, 0 warnings
- All layouts use FluentLayout, FluentBodyContent, FluentNavMenu, FluentCard
- CSS variables already in use (e.g., `var(--neutral-stroke-rest)`)
- Blazored.LocalStorage is already installed and available for theme persistence

### ThemeService Component Specification
[Source: architecture/5-component-architecture.md#5.2]
- **Responsibility:** Manage theme selection, light/dark mode toggle, CSS variable injection
- **Integration Points:** LocalStorage, SiteSettings, Layout components
- **Key Interfaces:**
  - `GetCurrentTheme(): string`
  - `SetTheme(themeName: string): void`
  - `ToggleDarkMode(): void`
  - `GetAvailableThemes(): List<ThemeInfo>`
- **Dependencies:** Blazored.LocalStorage (already installed), SiteSettings repository
- **Technology Stack:** C# service + CSS custom properties + Fluent UI theming

### CSS Variables Reference from mockups/styles.css

**Color Palette (Light Theme Default):**
```css
--color-primary: #0078D4;
--color-primary-hover: #106EBE;
--color-secondary: #6B6B6B;
--color-accent: #107C10;
--color-background: #FFFFFF;
--color-surface: #F5F5F5;
--color-surface-hover: #EBEBEB;
--color-success: #107C10;
--color-warning: #FFB900;
--color-error: #D13438;
--color-neutral: #323130;
--color-neutral-light: #605E5C;
--color-border: #E1E1E1;
--color-link: #0078D4;
```

**Color Palette (Dark Theme):**
```css
--color-primary: #4DA6FF;
--color-primary-hover: #6CB8FF;
--color-secondary: #A0A0A0;
--color-accent: #54B054;
--color-background: #1F1F1F;
--color-surface: #2D2D2D;
--color-surface-hover: #3D3D3D;
--color-success: #54B054;
--color-warning: #FFD966;
--color-error: #FF6B6E;
--color-neutral: #E0E0E0;
--color-neutral-light: #A0A0A0;
--color-border: #404040;
--color-link: #4DA6FF;
```

**Typography:**
```css
--font-family: 'Segoe UI', -apple-system, BlinkMacSystemFont, sans-serif;
--font-family-mono: 'Cascadia Code', Consolas, monospace;
--font-size-h1: 2rem;
--font-size-h2: 1.5rem;
--font-size-h3: 1.25rem;
--font-size-body: 1rem;
--font-size-small: 0.875rem;
--font-size-xs: 0.75rem;
--font-weight-regular: 400;
--font-weight-semibold: 600;
--line-height-tight: 1.25;
--line-height-normal: 1.6;
```

**Spacing Scale (4px base):**
```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 16px;
--spacing-lg: 24px;
--spacing-xl: 32px;
--spacing-2xl: 48px;
--spacing-3xl: 64px;
```

**Layout:**
```css
--max-content-width: 1200px;
--sidebar-width: 280px;
--header-height: 56px;
--border-radius: 4px;
--border-radius-lg: 8px;
```

**Shadows:**
```css
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.1);
--shadow-md: 0 2px 8px rgba(0, 0, 0, 0.12);
--shadow-lg: 0 8px 16px rgba(0, 0, 0, 0.14);
```

**Transitions:**
```css
--transition-fast: 150ms ease;
--transition-normal: 200ms ease-out;
```

### Theme Selection Attributes
- `data-theme`: Controls light/dark mode ("light" | "dark")
- `data-site-theme`: Controls theme selection ("fluent-modern" | "developer" | "minimal")
- System preference detection via `@media (prefers-color-scheme: dark)`

### Developer Dark Theme Key Differences
[Source: mockups/theme-developer.css]
- Monospace typography: JetBrains Mono, Fira Code, Cascadia Code
- GitHub-inspired light colors, VS Code-inspired dark colors
- Includes syntax highlighting variables (--syntax-keyword, --syntax-string, etc.)
- Terminal/code-block styling with ">" prefix on logo

### Minimal Clean Theme Key Differences
[Source: mockups/theme-minimal.css]
- Serif typography: Charter, Georgia, Playfair Display for headings
- Paper-like warm backgrounds
- Generous whitespace, narrower article max-width (680px)
- Drop cap styling for article first paragraphs

### File Locations
[Source: architecture/source-tree.md#7.2]
```
source/BlogUI/
├── Themes/                 # NEW: CSS variable theme files
│   ├── _variables.css      # Base CSS variables
│   ├── fluent-modern.css   # Theme 1 (default)
│   ├── developer-dark.css  # Theme 2
│   └── minimal-clean.css   # Theme 3
├── Components/
│   └── ThemeProvider.razor # NEW: Theme provider component
├── Common/
│   └── ThemeService.cs     # NEW: Theme management service
└── Layouts/                # Update to use CSS variables
```

### Coding Standards
[Source: architecture/coding-standards.md]
- **CSS Variables:** All colors, fonts, spacing via CSS custom properties only
- **XML Documentation:** Every class and method MUST have XML documentation comments
- **Fluent UI Exclusive:** Use Fluent UI components exclusively
- **File Naming:** PascalCase for .cs and .razor files, kebab-case for .css files

## Testing

### Test File Location
`TechieBlog.Tests/` (to be created in Story 1.14)

### Testing Standards
- Framework: xUnit
- Coverage Target: 80% for BlogEngine, 60% for BlogUI components
- No test project exists yet - manual testing required for this story

### Story-Specific Testing Requirements
1. Verify all three themes load without CSS errors
2. Verify light/dark mode toggle persists across page reloads
3. Verify theme selection persists across sessions (LocalStorage)
4. Verify CSS variables apply correctly to all UI components
5. Verify no unstyled content flash on page load
6. Verify theme CSS files are properly loaded in _Host.cshtml

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-12-18 | 1.0 | Initial story draft | Bob (SM Agent) |
| 2025-12-18 | 1.1 | Story validated and approved - all sources verified | Sarah (PO Agent) |
| 2025-12-18 | 1.2 | Implementation complete - all tasks done, build passes | James (Dev Agent) |
| 2025-12-18 | 1.3 | QA Review PASS - Quality Score 100/100 | Quinn (QA Agent) |
| 2025-12-18 | 1.4 | Story marked Done | Bob (SM Agent) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References
- Build verification: `dotnet build` - 0 errors, 0 warnings
- CSS files placed in `wwwroot/Themes/` for RCL static content serving
- Fixed nullable annotation warnings (CS8632, CS8669) by removing `?` from event delegate
- Fixed unused field warning (CS0414) by removing `isInitialized` field

### Completion Notes List
1. Created comprehensive CSS variable system in `_variables.css` with full documentation
2. Implemented three themes: Fluent Modern (default), Developer Dark, Minimal Clean
3. Each theme supports light/dark mode via `data-theme` attribute
4. ThemeService provides full theme management with LocalStorage persistence
5. ThemeProvider component wraps app content and applies theme via JS Interop
6. FOUC prevention script in _Host.cshtml reads theme from localStorage before render
7. All layouts updated to use CSS custom properties for consistent theming
8. System preference detection via `prefers-color-scheme` media query

### File List
**New Files Created:**
- `source/BlogUI/wwwroot/Themes/_variables.css` - Base CSS custom properties
- `source/BlogUI/wwwroot/Themes/fluent-modern.css` - Default Fluent UI theme
- `source/BlogUI/wwwroot/Themes/developer-dark.css` - Code editor inspired theme
- `source/BlogUI/wwwroot/Themes/minimal-clean.css` - Typography-focused theme
- `source/BlogUI/Common/ThemeService.cs` - Theme management service
- `source/BlogUI/Components/ThemeProvider.razor` - Theme provider component

**Files Modified:**
- `source/TechieBlog/Program.cs` - Added ThemeService registration
- `source/TechieBlog/Pages/_Host.cshtml` - Added CSS links and FOUC prevention
- `source/BlogUI/App.razor` - Wrapped content in ThemeProvider
- `source/BlogUI/Layouts/MainLayout.razor` - Updated to use CSS variables
- `source/BlogUI/Layouts/AdminLayout.razor` - Updated to use CSS variables
- `source/BlogUI/Layouts/AuthLayout.razor` - Updated to use CSS variables

## QA Results

### Review Date: 2025-12-18

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall Grade: Excellent**

The CSS Theme Infrastructure implementation demonstrates high-quality code with excellent documentation, proper separation of concerns, and adherence to coding standards. The implementation successfully delivers all 7 acceptance criteria with well-structured CSS custom properties, three distinct themes, and a robust theme management system.

**Key Strengths:**
- Comprehensive documentation in all CSS and C# files
- Clean architecture: base variables → theme overrides → service → provider
- Proper error handling for JS interop edge cases (prerendering, circuit disconnect)
- Event-based reactivity for theme changes
- FOUC prevention via inline script in _Host.cshtml
- Correct RCL static content placement (wwwroot/Themes/)

### Refactoring Performed

None required. Code quality meets all standards.

### Compliance Check

- Coding Standards: ✓ XML docs on all public members, CSS variables for styling, correct naming conventions
- Project Structure: ✓ Files in correct locations per source-tree.md, CSS in wwwroot for RCL
- Testing Strategy: ✓ Manual testing requirements defined (no test project exists yet per Story 1.14)
- All ACs Met: ✓ All 7 acceptance criteria verified and implemented

### Improvements Checklist

[All items already addressed by development - no outstanding issues]

- [x] Base CSS variables file with comprehensive documentation
- [x] Fluent Modern theme with light/dark variants and system preference detection
- [x] Developer Dark theme with syntax highlighting and monospace typography
- [x] Minimal Clean theme with serif typography and drop cap styling
- [x] ThemeService with LocalStorage persistence and event-based updates
- [x] ThemeProvider component with JS interop for DOM manipulation
- [x] Layout integration with CSS variables
- [x] FOUC prevention in _Host.cshtml
- [x] Build verification: 0 errors, 0 warnings

### Security Review

**Status: PASS**

No security concerns. Theme preferences stored in LocalStorage are non-sensitive user preferences. No authentication, authorization, or data protection requirements for this story.

### Performance Considerations

**Status: PASS**

- CSS files loaded statically with appropriate cascade order
- Minimal JavaScript (FOUC prevention script, theme attribute setter)
- Theme changes applied via DOM attribute manipulation (efficient)
- No runtime CSS generation or expensive operations

### Files Modified During Review

None. No modifications required.

### Gate Status

Gate: **PASS** → docs/qa/gates/1.5-css-theme-infrastructure.yml
Quality Score: 100/100

### Recommended Status

✓ **Ready for Done**

All acceptance criteria met, code quality excellent, build passes, documentation complete.
